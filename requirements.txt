fastapi==0.111.0
uvicorn[standard]==0.29.0
google-genai>=0.6.0
google-api-python-client==2.128.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0
pydantic>=2.0.0
python-multipart==0.0.9
pyttsx3==2.90

# On Windows avoid the GTK extra which requires building PyGObject.
# Use the plain pywebview package which picks a suitable backend on Windows.
pywebview

# ── Coqui TTS (voice cloning) ──────────────────────────────
# Install separately if you want voice cloning:
#   pip install TTS
# Requires ~4GB disk + CUDA recommended but CPU works
# Drop a 10-30s WAV file of Irrfan's voice in:
#   voice/irrfan_sample.wav

pywebview>=5.0
